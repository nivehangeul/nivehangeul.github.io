name: Hugo site CI

on:
  push:
    branches:
      - test

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the site in the Hugo/builder container
      run: |
        cd hugo-blog
        ls -a
